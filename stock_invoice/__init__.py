#from . import wizard
from . import models
#from . import controllers

from odoo import fields, models, api


# def post_init_hook(cr, registry):
#     cr.execute("""UPDATE account_move_line SET is_migrated = 'f'""")
#     cr.execute("""UPDATE account_move SET is_migrated = 'f'""")
#     cr.execute("""UPDATE account_account SET is_migrated = 'f'""")
